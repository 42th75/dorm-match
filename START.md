# 처음부터 따라하기

프로그래밍이나 깃허브를 한 번도 안 써봤다는 전제로 썼다.
**위에서부터 순서대로** 하면 된다. 한 단계가 끝날 때마다 "이게 보이면 성공"이 적혀 있으니
그게 안 보이면 다음으로 넘어가지 말고 아래 '막혔을 때'를 본다.

전부 다 하는 데 **처음이면 40분쯤** 걸린다.

---

## 준비물

두 가지만 깔면 된다. 둘 다 무료다.

| | 무엇 | 어디서 |
|---|---|---|
| 1 | **VS Code** — 코드를 쓰고 고치는 프로그램 | https://code.visualstudio.com |
| 2 | **Git** — 파일을 깃허브로 보내는 도구 | https://git-scm.com/downloads |

그리고 **깃허브 계정**이 필요하다. https://github.com 에서 가입한다.

> Git 설치할 때 창이 여러 번 뜨는데 전부 **Next** 를 눌러도 된다. 기본 설정이면 충분하다.

**이게 되면 성공** — VS Code를 켜고 위쪽 메뉴에서 `터미널 → 새 터미널`을 누른 뒤
아래 칸에 `git --version` 을 치고 엔터. `git version 2.xx.x` 같은 글자가 나오면 된다.

---

## 1단계 · 폴더 열기

1. 내려받은 압축 파일을 풀면 `dorm-match` 폴더가 나온다. 바탕화면이나 문서함 같은
   찾기 쉬운 곳에 둔다.
2. VS Code를 켜고 `파일 → 폴더 열기` 를 눌러 그 폴더를 고른다.
3. "이 폴더에 있는 파일 작성자를 신뢰합니까?" 라고 물으면 **신뢰함**을 누른다.

왼쪽에 이런 목록이 보이면 된다.

```
DORM-MATCH
├── css
├── data
├── js
├── index.html
├── README.md
└── START.md      ← 지금 읽고 있는 파일
```

**이게 되면 성공** — 왼쪽에 위 목록이 보인다.

---

## 2단계 · 내 컴퓨터에서 열어 보기

아직 인터넷에 올리기 전에, 잘 돌아가는지 먼저 본다.

1. VS Code 왼쪽 세로줄에서 **네모 네 개 모양 아이콘**(확장)을 누른다.
2. 검색칸에 `Live Server` 를 치고, 만든 사람이 **Ritwick Dey** 인 것을 **설치**한다.
3. 왼쪽 파일 목록에서 `index.html` 을 **우클릭 → Open with Live Server**.

브라우저가 열리면서 사이트가 뜬다. 주소는 `http://127.0.0.1:5500` 같은 모양이다.

> `index.html` 을 그냥 두 번 눌러서 열면 안 된다. 브라우저 규칙 때문에 자료 파일을
> 읽지 못한다. 꼭 Live Server로 연다.

**이게 되면 성공** — 브라우저에 "기숙사 룸메이트 배정"이 뜨고,
`아직 배정 결과가 공개되지 않았다` 라고 나온다. 아직 아무 자료도 없으니 정상이다.

오른쪽 위 **관리자 로그인**을 눌러 비밀번호 `1234` 를 넣어 본다.
위쪽에 **응답 불러오기**와 **배정 실행** 탭이 새로 생기면 제대로 도는 것이다.

---

## 3단계 · 비밀번호와 제목 바꾸기

1. 왼쪽 목록에서 `js` 폴더를 누르고 `config.js` 를 연다.
2. 아래 세 줄을 고친다.

```js
const CONFIG = {
  title: "기숙사 룸메이트 배정",     ← 학교 이름 등으로 바꾼다
  subtitle: "2026학년도 1학기",      ← 학기를 적는다
  adminPassword: "1234",            ← 반드시 바꾼다
};
```

3. `Ctrl + S` (맥은 `Cmd + S`) 로 저장한다. 브라우저가 알아서 새로고침된다.

> **비밀번호는 숨겨지지 않는다.** 정적 사이트라 누구나 소스를 열면 볼 수 있다.
> 이건 실수로 남의 자료를 건드리는 걸 막는 잠금이지 보안 장치가 아니다.
> 그래서 다른 곳에서 쓰는 비밀번호를 절대 쓰지 않는다.

**이게 되면 성공** — 브라우저 왼쪽 위 제목이 바뀌어 있다.

---

## 4단계 · 깃허브에 올리기

여기가 처음이면 제일 낯선 부분인데, 실제로는 버튼 몇 개다.

### 4-1. 깃허브에 빈 저장소 만들기

1. https://github.com 에 로그인하고 오른쪽 위 **+** → **New repository**.
2. **Repository name** 에 `dorm-match` 를 적는다.
3. **Public** 을 고른다. (Private이면 무료 계정에서 사이트 공개가 안 된다)
4. 아래 체크박스는 **아무것도 건드리지 않는다.** README 추가 같은 것도 켜지 않는다.
5. **Create repository** 를 누른다.

다음 화면에 나오는 주소를 복사해 둔다. 이런 모양이다.

```
https://github.com/내아이디/dorm-match.git
```

### 4-2. VS Code에서 보내기

VS Code로 돌아와 `터미널 → 새 터미널` 을 연다. 아래 칸에 **한 줄씩** 치고 엔터를 누른다.

처음 한 번만 이름과 메일을 알려 준다. 깃허브 가입할 때 쓴 것을 적는다.

```bash
git config --global user.name "내이름"
git config --global user.email "내메일@example.com"
```

이제 올린다.

```bash
git init
git add .
git commit -m "룸메이트 배정 사이트"
git branch -M main
git remote add origin https://github.com/내아이디/dorm-match.git
git push -u origin main
```

마지막 줄에서 로그인 창이 뜬다. **Sign in with your browser** 를 눌러 깃허브 계정으로
로그인하면 된다.

**이게 되면 성공** — 깃허브의 내 저장소 페이지를 새로고침하면 파일들이 올라와 있다.

### 4-3. 사이트로 공개하기

1. 깃허브 저장소 페이지에서 위쪽 **Settings** 를 누른다.
2. 왼쪽 목록에서 **Pages** 를 누른다.
3. **Source** 를 `Deploy from a branch` 로 둔다.
4. **Branch** 를 `main`, 폴더를 `/ (root)` 로 고르고 **Save**.
5. 1~2분 기다렸다가 페이지를 새로고침하면 위에 주소가 나온다.

```
https://내아이디.github.io/dorm-match/
```

**이게 되면 성공** — 그 주소를 폰으로 열어도 사이트가 뜬다. 이제 친구들에게 이 주소를 주면 된다.

---

## 5단계 · 구글 폼 만들기

1. https://forms.google.com 에서 새 폼을 만든다.
2. 오른쪽 위 톱니바퀴 → **응답** → **이메일 주소 수집** 켜기,
   **응답 수정 허용** 켜기.
3. 아래 9개 문항을 그대로 넣는다. **질문 문장을 조금 바꿔도 프로그램이 알아서 찾는다.**
   낱말을 통째로 바꾸지만 않으면 된다.

| # | 질문 | 형식 | 보기 |
|---|---|---|---|
| 1 | 학번 | 단답형 | |
| 2 | 이름 | 단답형 | |
| 3 | 평일에 보통 몇 시에 잠드나요? | 객관식 | 24시 이전 / 24:00–00:30 / 00:30–01:00 / 01:00–01:30 / 01:30–02:00 / 02시 이후 |
| 4 | 평일에 보통 몇 시에 일어나나요? | 객관식 | 7시 이전 / 7:00–7:20 / 7:20–7:40 / 7:40–8:00 / 8시 이후 |
| 5 | 옆에서 나는 작은 소리나 불빛 때문에 잠을 설치나요? | 선형 배율 1~5 | 1 = 웬만해선 안 깬다 · 5 = 작은 소리에도 깬다 |
| 6 | 책상이나 바닥에 물건이 며칠씩 쌓여 있는 편인가요? | 선형 배율 1~5 | 1 = 바로바로 치운다 · 5 = 자주 쌓아둔다 |
| 7 | 잘 때 방 온도는 어느 쪽이 좋나요? | 선형 배율 1~5 | 1 = 서늘한 게 좋다 · 5 = 따뜻한 게 좋다 |
| 8 | 룸메이트를 정할 때 가장 중요한 것 하나를 고른다면? | 객관식 | 취침·기상 시간 / 방의 조용함 / 정리 정돈 / 방 온도 / 크게 상관없다 |
| 9 | 같은 방이 되면 곤란한 학생이 있나요? (학번만) | 단답형, **필수 아님** | |

> 3, 4번 보기는 우리 학교 사정에 맞춰 좁게 잡았다. 취침은 12시 이후, 기상은 7시 이후가
> 기본이라 30분 단위로 나눠야 사람들이 실제로 갈라진다. 기숙사 규정이 바뀌면
> `js/matcher.js` 위쪽 `FIELDS` 에서 보기와 숫자를 같이 고친다.
>
> 5, 6, 7번 선형 배율은 **양 끝 설명을 반드시 적는다.** 안 적으면 다들 가운데인 3을 고른다.
>
> 9번 아래 설명란에 이렇게 적어 둔다:
> *"학번만 적어 주세요. 이유는 쓰지 않아도 됩니다. 여기 적은 내용은 배정 담당자 외에는
> 아무도 볼 수 없고, 결과 안내에도 나타나지 않습니다."*

---

## 6단계 · 배정 돌리기

응답이 다 모이면 이렇게 한다.

1. 구글 폼 → **응답** 탭 → 오른쪽 위 초록색 **스프레드시트 아이콘** 을 눌러 시트를 연다.
2. 시트에서 `Ctrl + A` (전체 선택) → `Ctrl + C` (복사).
3. 우리 사이트를 열고 **관리자 로그인**.
4. **응답 불러오기** 탭의 큰 칸에 `Ctrl + V` 로 붙여넣고 **읽어들이기**.
5. 아래에 나오는 **열 맞추기** 표를 확인한다. 빨간 화살표가 있으면 그 줄에서
   올바른 열을 직접 고르고 **이 설정으로 다시 읽기**.
6. **배정 실행** 탭 → **배정 계산하기**.

결과 위에 숫자 다섯 개가 나온다. 그중 두 개만 보면 된다.

- **생활차이 큰 학생** — 룸메와 생활 시간이나 습관이 꽤 어긋난 학생 수다. 이 수를
  줄이는 것이 배정의 목표다. 80명이면 보통 3~5명쯤 나온다.
- **아주 큰 학생** — 0명이어야 정상이다. 1명 이상 나오면 응답에 극단적인 사람이
  있다는 뜻이니, 그 학생은 따로 확인해 보는 게 좋다.

---

## 7단계 · 결과 공개하기

배정을 마쳤으면 **깃허브에 바로 올릴 수 있다.** 파일을 옮기거나 커밋할 필요가 없다.

### 7-1. 처음 한 번만 · 깃허브 토큰 만들기

토큰은 이 사이트가 결과 파일을 저장소에 대신 올릴 수 있게 해 주는 열쇠다.
한 번만 만들어 두면 다음부터는 버튼만 누르면 된다.

1. https://github.com 에 로그인 → 오른쪽 위 프로필 사진 → **Settings**.
2. 왼쪽 맨 아래 **Developer settings** → **Personal access tokens** → **Fine-grained tokens**.
3. **Generate new token** 을 누른다.
4. **Token name** 은 아무 이름, **Expiration**(만료일)은 학기 끝나는 날쯤으로 둔다.
5. **Repository access** → **Only select repositories** → `dorm-match` 를 고른다.
6. **Permissions** → **Repository permissions** → **Contents** 를 **Read and write** 로 바꾼다.
7. 맨 아래 **Generate token** → 나오는 `github_pat_…` 를 복사한다.
   (이 화면을 벗어나면 다시 못 보니 바로 다음 단계로 간다)

> 토큰은 비밀번호와 같다. 남에게 보여주지 말고, 다른 곳에서 쓰던 걸 재사용하지 않는다.
> 이 토큰은 `dorm-match` 저장소의 파일을 고치는 것 말고는 아무것도 못 한다.

### 7-2. 공개하기

1. **배정 실행** 탭에서 배정을 마치면 아래에 **결과 공개하기** 가 나온다.
2. **깃허브 토큰** 칸에 위에서 복사한 토큰을 붙여넣는다.
   (이 브라우저에 저장되니 다음부터는 안 적어도 된다)
3. **깃허브에 바로 공개** 를 누른다.
4. "공개했다" 가 뜨면 끝이다.

1~2분 뒤 학생들이 사이트에서 자기 학번을 넣으면 결과가 보인다.

**이게 되면 성공** — 공개된 주소에서 "아직 공개되지 않았다"가 사라지고 실제 방 목록이 보인다.

> 인터넷이 안 되거나 토큰이 없을 땐 **대신 파일로 내려받기** 를 눌러 예전 방식
> (내려받아 `data/result.json` 에 넣고 VS Code에서 커밋·푸시)으로도 할 수 있다.

> 설문 원본과 기피 학번은 **관리자 브라우저에만** 남고 절대 인터넷에 올라가지 않는다.
> `result.json` 에는 방 목록과 짧은 근거만 들어 있다. 직접 열어서 확인해 봐도 된다.

---

## 막혔을 때

**Live Server를 눌렀는데 아무 일도 안 생긴다**
확장이 제대로 깔렸는지 본다. 오른쪽 아래에 `Go Live` 라는 글자가 있으면 그걸 눌러도 된다.

**"아직 배정 결과가 공개되지 않았다"고 나온다**
`data/result.json` 파일이 아직 없어서 그렇다. 7단계를 안 했으면 정상이다.

**`git push` 에서 로그인이 안 된다**
터미널에 `git remote -v` 를 쳐서 주소가 맞는지 본다. 틀렸으면
`git remote set-url origin 올바른주소` 로 고친다.

**깃허브에 올렸는데 주소를 열면 404가 뜬다**
Settings → Pages 설정을 다시 본다. Branch가 `main`, 폴더가 `/ (root)` 여야 한다.
방금 설정했다면 2분쯤 기다린 뒤 새로고침한다.

**"깃허브에 바로 공개" 가 안 된다 (401·403 오류)**
401이면 토큰을 잘못 붙여넣은 것이니 다시 복사해 넣는다. 403이면 토큰 권한이 모자란
것이다. 7-1에서 **Contents** 를 **Read and write** 로 했는지, `dorm-match` 저장소를
골랐는지 확인한다. 토큰 만료일이 지났으면 새로 만든다. 급하면 **대신 파일로 내려받기**
로 예전 방식으로 올린다.

**열 맞추기에서 자꾸 빨간 화살표가 뜬다**
구글 폼 질문에서 핵심 낱말이 빠졌을 수 있다. 표에서 직접 올바른 열을 고르면 그대로 쓰면 된다.

**"응답이 홀수다"라고 나온다**
2인실만 있어 짝이 맞지 않는다는 뜻이다. 남는 한 명은 혼자 쓰는 방으로 표시되니,
응답 인원을 짝수로 맞춰 다시 배정한다.

**"생활차이 큰 학생"이 10명이 넘는다**
응답이 한쪽으로 몰려 있을 수 있다. **응답 불러오기** 탭 아래쪽 문항별 분포를 본다.
막대 하나에 사람이 몰린 문항이 있으면 그 문항이 아무 구실을 못 하고 있는 것이니,
다음 학기에 보기를 더 잘게 나눈다.

---

## 다음에 또 쓸 때

새 학기에 다시 쓸 때는 4단계(깃허브 올리기)를 건너뛴다. 5단계부터 하면 된다.
구글 폼은 **사본 만들기**로 복제해서 쓰면 편하다.
